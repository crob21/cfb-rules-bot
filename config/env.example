# ===========================================
# CFB 26 Rules Bot - Environment Configuration
# ===========================================

# Required: Discord Bot
DISCORD_BOT_TOKEN=your_discord_bot_token_here
DISCORD_GUILD_ID=your_guild_id_here

# Optional: AI Integration (for intelligent responses)
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Player Lookup (CollegeFootballData.com)
CFB_DATA_API_KEY=your_cfb_data_api_key_here

# Optional: Web Scraping (Zyte API for Cloudflare bypass)
# Get your API key from: https://www.zyte.com/zyte-api/
# Cost: ~$0.233 per 1,000 requests (free tier available)
ZYTE_API_KEY=your_zyte_api_key_here

# Optional: Zyte Organization ID (for official usage tracking via Stats API)
# Required for `/admin zyte view:api` to show official Zyte usage
# Find your org ID in the dashboard URL: https://app.zyte.com/o/YOUR_ORG_ID
# Example: If URL is app.zyte.com/o/123456, use 123456
# Note: Uses the same ZYTE_API_KEY for authentication
ZYTE_ORG_ID=your_zyte_org_id_here

# Optional: Dashboard (Web Interface)
DISCORD_CLIENT_ID=your_discord_app_client_id
DISCORD_CLIENT_SECRET=your_discord_app_client_secret
DISCORD_REDIRECT_URI=http://localhost:8080/auth/callback
DASHBOARD_SECRET_KEY=generate_a_random_secret_key
DASHBOARD_PORT=8080

# ===========================================
# Storage Backend Configuration
# ===========================================
# Options: discord (default), supabase
# - discord: Free, stores in bot owner's DMs (good for <10 servers)
# - supabase: PostgreSQL database (scales to unlimited servers)
STORAGE_BACKEND=discord

# Supabase Configuration (only needed if STORAGE_BACKEND=supabase)
# 1. Create free project at supabase.com
# 2. Create table: configs (namespace TEXT, key TEXT, data JSONB, PRIMARY KEY (namespace, key))
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key

# ===========================================
# Bot Admins (hardcoded fallback)
# ===========================================
# Comma-separated Discord User IDs for permanent admins
BOT_ADMIN_IDS=123456789012345678,987654321098765432
